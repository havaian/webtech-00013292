// Creating Expense Model
const Expense_Model = (text, category, amount) => {
    this.text = text;
    this.category = category;
    this.amount = amount;
};

// export default Expense Model;
module.exports = Expense_Model;
